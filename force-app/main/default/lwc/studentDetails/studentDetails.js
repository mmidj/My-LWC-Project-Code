import { LightningElement } from 'lwc';

export default class StudentDetails extends LightningElement {

    student = {
        stName : "Isaac Gordon",
        id : "345564",
        city : "Dallas",
        favColor : "blue"
    }
}